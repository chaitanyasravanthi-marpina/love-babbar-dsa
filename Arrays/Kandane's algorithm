#include <bits/stdc++.h>
using namespace std;
int maxSubarraySum(vector<int> &arr)
{
    // Code here
    int n = arr.size();
    int sum = 0;
    int maxsum = INT_MIN;
    for (int i = 0; i < n; i++)
    {
        sum += arr[i];
        maxsum = max(maxsum, sum);
        if (sum < 0)
        {
            sum = 0;
        }
    }
    return maxsum;
}
